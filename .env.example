# Copy this file to `.env` (not committed) and fill values.
# Example values are placeholders — DO NOT commit real secrets.

FLASK_ENV=development
SECRET_KEY=change-me-to-a-random-string
DATABASE_URL=postgresql://user:password@host:5432/dbname
DB_SCHEMA=avto_bot
MASTER_SECRET_KEY=replace-with-generated-key

# Optional (leave blank unless you need a default OpenAI key)
OPENAI_API_KEY=

# For local testing you can use SQLite by setting DATABASE_URL like:
# DATABASE_URL=sqlite:///./dev.db

# To generate a Fernet master key:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
